U-Net .keras / .h5 files are not committed to Git (see repo .gitignore).
Place the same files you use locally here on your API machine, or set CHEXIT_ASSETS_ROOT
to a folder that contains models/ and tb_classifier_output/.
